import { SpliceDescreptionPipe } from './splice-descreption.pipe';

describe('SpliceDescreptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SpliceDescreptionPipe();
    expect(pipe).toBeTruthy();
  });
});
